#include "list.c"
#include <locale.h>
#include <stdio.h>
#include <stdlib.h>

int initialize_list(single_linked_list_t* list, int count, ...);

int main()
{
    // single_linked_list_t list = initialize_list(, )
    printf("W.I.P");
    return 0;
}
